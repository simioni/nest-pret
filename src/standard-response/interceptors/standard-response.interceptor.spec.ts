import { StandardResponseInterceptor } from './standard-response.interceptor';

describe('StandardResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new StandardResponseInterceptor()).toBeDefined();
  });
});
